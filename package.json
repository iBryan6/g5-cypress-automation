{
  "name": "cypress-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress open",
    "cypress:run:staging": "cypress run --env TESTING_ENV=staging --reporter mochawesome",
    "cypress:run:prime": "cypress run --env TESTING_ENV=prime --reporter mochawesome",
    "cypress:run:prod": "cypress run --env TESTING_ENV=prod --reporter mochawesome"
  },
  "author": "Bryan Argandona",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.3.0",
    "cypress-xpath": "^1.4.0"
  },
  "dependencies": {
    "mocha": "^10.2.0",
    "mochawesome": "^5.0.0"
  }
}
